{"domain":"picture_ocr","root_causes":[{"id":"RC1","root_cause":"_recover_picture_text skip gates (clip_text_already_exported and page_coverage) produce empty PictureResults for chart regions in PDFs: when a chart's text labels are already in the Docling markdown export (clip_text containment guard, converters.py L1881) or the region covers >60% of the page with a text layer (coverage threshold, L1849-1874), the region is skipped with only a skipped_reason. splice_figure_markers then strips the <!-- image --> marker entirely (STRIP_SKIPPED_IMAGE_MARKERS=true default), so route_and_extract_flat never creates an indexed image block. No figure_path, no ocr_text, no description on the stored flat doc. VLM_DESCRIBE_IMAGES=false (default) means no visual semantic fallback exists.","code_path":"converters.py:_recover_picture_text L1849-1898 (skip gates) -> _recover_picture_results L2148 (placeholder PictureResult) -> splice_figure_markers L2071-2075 (strip skipped markers) -> route_and_extract_flat L2335-2337 (no indexed image block created) -> _enrich_image_blocks L602-608 (nothing to enrich)","fix_category":"missing_feature","estimated_complexity":"medium","suggested_fix":"When a region is skipped with clip_text_already_exported or page_coverage, still retain the cropped png_bytes in the PictureResult so the figure PNG is persisted to MinIO and the image block maintains its semantic link to the chart content. Decouples 'text already extracted' from 'visual context already captured'. Alternatively, for clip_text_already_exported regions, propagate the clip_text as ocr_text on the PictureResult so splice_figure_markers emits a [Chart text] block linking the chart image to its data.","affected_files":["src/pageindex_mcp/converters.py","src/pageindex_mcp/client.py"],"affected_docs":["uae_numbers_english_page_16_17_portrait - Copy.pdf"],"confidence":"high","related_rfcs":["017-ocr-image-block-decoupling.md","024-run7-verdict-stability-and-recovery-gaps.md"]},{"id":"RC2","root_cause":"Standalone JPG image route creates synthetic PictureResult with png_bytes but ocr_text='' because D8a (RFC-023) skips standalone Tesseract OCR when Docling's md_content exceeds MIN_STANDALONE_IMAGE_MD_CHARS (100). The image block gets figure_path from _enrich_image_blocks but no ocr_text. Chart quantitative data (percentages, labels) ends up in separate prose blocks, not semantically linked to the image block. The audit counts zero PictureResult enrichments because it looks for '> [Chart text]:' patterns which require non-empty ocr_text on the PictureResult.","code_path":"client.py L928-930 (D8a gate skips standalone OCR when md_content > 100 chars) -> L938-946 (synthetic PictureResult with ocr_text='') -> splice_figure_markers L2081 (ocr is empty, no Chart text marker) -> _enrich_image_blocks L618-619 (sets ocr_text='' on block)","fix_category":"missing_feature","estimated_complexity":"small","suggested_fix":"For standalone images where Docling extracts meaningful text (>100 chars), copy the Docling-extracted text into the synthetic PictureResult's ocr_text field rather than leaving it empty. This ensures the image block carries the chart's textual content as ocr_text, and splice_figure_markers emits a [Chart text] block linking the visual image to its data. Guard against double-counting by checking if md_content already has the text as prose.","affected_files":["src/pageindex_mcp/client.py"],"affected_docs":["image pie chart about labor distribution in january 2025 - Copy.jpg"],"confidence":"high","related_rfcs":["017-ocr-image-block-decoupling.md"]}],"needs_doc_drilldown":[]}