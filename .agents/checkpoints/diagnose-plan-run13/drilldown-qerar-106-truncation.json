{"doc":"قرار مجلس الوزراء رقم (106) لسنة 2022 بشأن اللائحة التنفيذية للمرسوم بقانون اتحادي رقم (9) لسنة 2022 بشأن أعمال الخدمة المساعدة.pdf","symptom":"Run 12 MARGINAL (179 nodes / 26,027 chars, depth-0 flat hierarchy) regressed to Run 13 FAIL (11 nodes / 2,022 chars, ~92% content loss); extraction now starts at Article 4 and ends abruptly instead of covering the full 15-page executive regulation.","root_cause":"RFC-029 D4 (Task 3.3) 'keep-best' OCR-retry comparison in client.py (commit 08b6eea) replaced the previous unconditional `retry_wins = True` (post_retry_chars > pre_retry_chars branch) with a repeating-token-density guard. `_repeating_token_density()` (client.py:1083-1096) returns a hardcoded 0.0 whenever the input has fewer than 20 alnum tokens (line 1094). This doc is a 15-page fully scanned/image-only PDF (confirmed: PyPDF2 extracts 0 chars on every page) that is well under MAX_DOCLING_PAGES=150, so it never enters the chunked-Docling route — the truncation is not a chunking artifact. Instead it takes the LOW_CONTENT_OCR_CHAR_FLOOR retry path (client.py:1001-1014): the first, non-forced Docling pass yields near-empty/garbled markdown (a short mid-document fragment happening to start around 'Article 4'), reason='node_count<3', total_chars<300, triggering the force_full_page_ocr escalation. The escalation legitimately recovers far more content (post_retry_chars > pre_retry_chars), landing in the `else` branch at client.py:1111-1143. There, `_pre_garble_flag = _is_garbled_blob(pre_retry_text)` is true for the short garbled fragment, so the code computes `_pre_density = _repeating_token_density(pre_retry_text)`. Because the pre-retry fragment has <20 alnum tokens, `_pre_density` is forced to 0.0 regardless of its actual content. The subsequent check `_density_improved = _post_density < _pre_density * 0.80` then evaluates as `_post_density < 0.0`, which is unsatisfiable for any non-negative density value — `retry_wins` is unconditionally False whenever the pre-retry fragment is short-but-garbled. `result = pre_retry_result` (client.py:1145) then discards the complete, OCR-recovered markdown and keeps the short pre-retry garbled fragment (whatever partial/garbled text the first Docling pass happened to pull off a mid-document page range) as the persisted content — exactly matching the observed 'starts at Article 4, ends abruptly' truncation and the 179->11 node / 26,027->2,022 char collapse. This is a distinct code-level mechanism from the cluster's converters.py _repair_docling_tables/NFKC total-collapse finding: it lives in client.py's RFC-029 D4 retry-arbitration logic, not in the markdown-generation/table-repair code path, and produces partial-fragment truncation rather than 0-block total collapse.","code_path":"src/pageindex_mcp/client.py:1083-1147 (function-local `_repeating_token_density` at 1083-1096, retry-arbitration branch at 1098-1147, specifically the short-text degenerate case at line 1094 `if len(tokens) < 20: return 0.0` feeding the unsatisfiable comparison at line 1131 `_density_improved = _post_density < _pre_density * 0.80`)","fix_category":"bug","estimated_complexity":"small","suggested_fix":"In `_repeating_token_density`, do not silently collapse short inputs to 0.0 density (which biases the RFC-029 D4 comparison to always reject the retry). Either (a) return `None`/a sentinel for <20-token inputs and treat that case as 'density undecidable -> retry_wins = True' (mirroring the pre-existing behavior before RFC-029 D4), since a growth in char count from a too-short-to-score fragment to a scoreable OCR result is itself strong evidence of recovery; or (b) fall back to `_is_garbled_blob` post-retry check alone (as already done in the equal-char-count tie-break branch at line 1100-1110) when the pre-retry token count is below the 20-token floor. Add a regression test with a <20-token garbled pre-retry fragment and a much longer, non-garbled post-retry OCR result to assert retry_wins=True.","affected_files":["src/pageindex_mcp/client.py"]}