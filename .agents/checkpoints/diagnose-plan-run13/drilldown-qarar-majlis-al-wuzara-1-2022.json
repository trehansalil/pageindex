{"doc":"قرار مجلس الوزراء رقم (1) لسنة 2022 في شأن اللائحة التنفيذية للمرسوم بقانون اتحادي رقم (33) لسنة 2021 بشأن تنظيم علاقات العمل.pdf","verdict_run12":"MARGINAL","verdict_run13":"MARGINAL","chars_run12":48050,"chars_run13":14865,"nodes_run12":349,"nodes_run13":129,"pdf_pages":21,"pdf_text_layer":"none (scanned, verified via PyMuPDF get_text() == '' on all 21 pages)","cluster_reassignment":"converter (OCR-retry keep-best logic), NOT splitter","root_cause_file":"src/pageindex_mcp/client.py","root_cause_function":"index() Fix-3 OCR-escalation 'keep-best' guardrail (RFC-029 D4 / Task 3.3)","root_cause_lines":"1083-1148 (esp. 1093-1096, 1111-1143)","root_cause_summary":"_repeating_token_density() returns 0.0 whenever a text has <20 alnum tokens (line 1094-1095). The RFC-029 D4 keep-best comparator (added since Run 12) computes _pre_density=_repeating_token_density(pre_retry_text) and only accepts the OCR-retry's richer result when _post_density < _pre_density*0.80. For scanned/no-text-layer PDFs like this one, the pre-retry pass (do_ocr=False by default, RFC-021 QF1 deferral) produces near-empty/garbled text, so pre_retry token count is almost always <20, forcing _pre_density=0.0. The inequality _post_density < 0.0*0.80 is then arithmetically impossible to satisfy, so retry_wins=False unconditionally whenever the pre-retry snapshot is short-and-garbled -- exactly the population of documents this OCR-escalation retry exists to rescue. On revert (line 1144-1147) only result/ok/reason are rolled back to the pre-retry (near-empty) tree; md_content, pic_results and tmp_md_path are NOT reverted, so the tree-path and flat-path diverge on which content survives depending on whether the pre-retry reason was node_count<3 (flat routing still uses the good md_content) or garbling (raises outright). For this document the net effect is partial content survival (14.8k of an original ~48k), consistent with only part of the OCR-recovered structure surviving the reverted tree gate while flat-routing salvages a fraction via a separate, less request-specific path.","affected_files":["src/pageindex_mcp/client.py"],"suggested_fix":"Guard _repeating_token_density (or its D4 caller) so a pre-retry text with <20 tokens is treated as 'no reliable density signal' rather than density=0.0 -- e.g. return None and short-circuit retry_wins=True (any real recovery beats near-empty content) instead of running it through the '<' comparison that is unsatisfiable against a floor of 0. Also make the revert at line 1144-1147 restore md_content/tmp_md_path/pic_results in lockstep with result/ok/reason so tree-path and flat-path never diverge on which markdown was actually used.","confidence":"high (verified against source at client.py:1083-1148, helpers.py:863-891 for _is_garbled_blob empty-blob=True, and confirmed via PyMuPDF that all 21 pages carry zero embedded text)"}
